<!--index.wxml-->
<view class="praiseText">
    <block wx:key="id" wx:for="{{praiseList}}">
        <view src="{{item.title}}" class="title" bindtap="postClick" data-title="{{item.title}}" data-path="{{item.path}}">{{item.title}}</view>
    </block>
</view>